import React from 'react';
import ReactDOM from 'react-dom';

import data from './testData.json';

ReactDOM.render(
    React.createElement('h2', null, 'Hello Chronos!'),
     document.getElementById('root')
);
